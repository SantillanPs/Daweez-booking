Nothing built yet — three ways to fix it, drawn at your 220px column.

What is actually awkward there, as I read it:

1. The box says the same money three times — *Whole stay total ₱1,250*, *Received ₱1,250*, then *Fully paid ✓*.
2. The action is a separate bare white pill floating under the box, so it reads as a leftover instead of the next thing to do.
3. The two containers disagree — one is a green-edged card, the other a plain outline.

- **A (recommended)** — one card: the settled box says it once (Fully paid ✓ + ₱1,250 + payment count), and the action becomes the box's own footer under a hairline. Check in takes the same place inside the box when nothing is owed.
- **B** — least change: keep two boxes, stop repeating the money, and make the action solid charcoal instead of a bare outline.
- **C** — tightest: the whole settled state is one row inside one box — state on the left, a small gold action on the right.

The owed state (method list · reference · gold Record button) is untouched in all three.

Say A, B or C — or tell me what you would change in one of them.
