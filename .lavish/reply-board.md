Redrawn as tiles — no paragraphs.

- **Left column (To build)** = the 18, coloured by priority. The two **red** ones (k140, k130) are the SQL holds: they only save in this browser until the two `.sql` files run.
- **Being built** = k70 restaurant/bar, with its follow-ups (k105 · k109 · k152).
- **Owe you a look** = the three in Review, each with a one-line "try it".
- **Closed** = your nine.

The four numbered tiles at the top are the order I would take: **run the SQL → test one stay → close the three → build k120 + k124**.

Tell me the number to start on.
