Built — and you were right about B's leftover space: drawing it that way was silly.

Done: the green box starts **right after the name** and runs to the **right edge**. `Fully paid ✓ / ₱1,250 received` at its left end, `Check out` at its right end. Nothing is left over on either side, and the name row uses its whole width.

Nothing else changed: same heights, same panel width (512px), same block rows underneath. `Check in` (green) takes the same slot before arrival.

Refresh the calendar and open the booking. If the green strip feels too wide now, say the word and I will bring the panel in to 448px as well (option C) — that is the only knob left.
