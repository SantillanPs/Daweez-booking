My mistake — I read your note as "make everything smaller" and changed heights too. I have put all of that back: the panel is exactly as you see it now. Nothing has been built from this page.

This page changes **left-to-right space only**; nothing gets shorter or taller. The dashed bar is empty space.

- **A (recommended)** — the green box grows to fill the row: `Fully paid ✓ / ₱1,250 received` on its left, `Check out` on its right. The gap disappears because the green area is where the gap was.
- **B** — the green box sits right after "SEB" with a normal gap; the empty space ends up at the far right of the row instead.
- **C** — the panel comes in from 512px to 448px (400px if you want it tighter). Nothing moves; every gap is simply shorter.

One thing I deliberately did **not** touch: the block rows (Payment receipts / Guest tab / Extend stay) also have an empty right half, and putting their grey line *beside* the title would use it — but that also makes each row shorter, which is vertical. Say the word if you want it and I will.

Tell me A, B, C or a mix. I will not build until you say.
