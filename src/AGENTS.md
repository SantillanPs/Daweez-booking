# Frontend (React + Vite Client) AGENTS.md

## Purpose

This directory contains the React + TypeScript frontend codebase for the Daweez Pension House property management system (PMS) staff dashboard and the guest-facing direct reservation portal.

## Ownership

- Primary Owner: Frontend Engineers / Antigravity Agent
- Scope: Handles UI rendering, user interaction flows, client-side state/caching, local storage fallback database, and API interactions with Supabase.

## Local Contracts

- **State Management & Routing**: Uses React state coupled with `@tanstack/react-query` to handle fetching, caching, and mutating, `@tanstack/react-router` for view-tab routing and layout hierarchies, and `@tanstack/react-table` for sortable, searchable data grids. Booking data is synchronized instantly over a WebSocket using a Supabase Realtime subscription via `useRealtimeBookings.ts` to replace high-frequency polling.
- **Core Components & Router Tree**:
  - [router.tsx](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/router.tsx) - Sets up routes (`/login`, `/reserve`, `/`, `/calendar`, `/bookings`, `/guests`, `/settings`, `/analytics`) and handles redirects.
  - [PublicReservePortal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/PublicReservePortal.tsx) - Guest-facing reservation page with live availability checking, explicit Regular/Promo pricing from the rate card (regular crossed-out + promo badge only while the global promo is ON), and GCash downpayment submission. Uses the shared React Query cache (`getBookings`/`getRooms`/`getVenues`), the unified `calculatePricing({ usePromo })` engine (so portal and staff invoices match), reacts to the `promoMode` storage event, stores `payment_reference` properly, applies a 30-minute hold (`expires_at`) so abandoned reservations free the room, and **records the online downpayment as a numbered `PaymentRecord`** (`utils/receiptNumber.ts`) so the money taken online has a reprintable receipt and the booking never looks unpaid to staff.
  - [DashboardLayout](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/DashboardLayout.tsx) - Responsive PMS layout shell (header, global **Promo ON/OFF** toggle persisted in `localStorage` (`daweez_promo_active`) and broadcast via `storage`/`promo-toggle` events, desktop/mobile bottom tabs, context provider).
  - [CalendarTab](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/CalendarTab.tsx) - Orchestrates the calendar page: month toolbar + legend, the room×day timeline grid, group selection, and the single booking slide-over. Clicking a booking opens `ExtendStayModal`; clicking an empty day (or New booking, or editing) opens the single `WalkInBookingForm`; the ⋯ More → Corporate action opens the separate `CorporateBookingForm`.
  - Calendar subcomponents live under `src/components/calendar/`:
    - [TimelineGrid](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/TimelineGrid.tsx) + [TimelineCell](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/TimelineCell.tsx) — memoized room×day grid; pills sit on the nights slept, every booking block carries a payment dot (coral = owes, amber = deposit only, green = paid), and the day each guest checks out gets a small rose "out" mark. A hover highlight lights only the hovered **room/venue label** (left) and the **day header** (top) — no line tints through the grid — so staff can read the date/room at a glance without tracing.
    - [WalkInBookingForm](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/WalkInBookingForm.tsx) — the **single booking form**, used for New booking, clicking an empty day, group booking, and editing. (The old `QuickBookingSheet` slide-over and its step components were removed.)
    - [ExtendStayModal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/ExtendStayModal.tsx) — the reservation slide-over (opens when clicking a booking). Laid out as a **folio, not a stack of cards**: header leads with the **unit** (+ the booking status chip **and a payment status chip** — `Owes` / `Partial` / `Paid`, the words for what the calendar's payment dot colour means, so staff read the state instead of decoding a dot), then the guest block (name, contact, companion chips, booked-from / paper-ref meta), then **one focal money card** — [BookingMoneyPanel](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/BookingMoneyPanel.tsx) — **always the same two figures, never folded**: **Whole stay total** — named that way because a bare "Total price" could not be told apart from the room price — and **Amount to pay** = `amountToPayNow` (the agreed deposit while nothing is paid, the outstanding rest once the deposit is in), each with a one-line caption saying what it covers (`Every charge, before any payment` / `Deposit (50%)` → `Rest of the stay`). It is a **split panel, not a loose paragraph**: a thin header strip carries the payment chip and the method, one hairline divides the two figures so they read as a pair, and the amount to be paid now sits in the **gold cell** — the figure staff act on — which also keeps the middle of a wide card from reading as empty space. **Under the figures sits the payment progress bar** — an empty grey track when nothing is in, a green fill for what has been received — plus a caption naming the money in (`✓ ₱1,250 of ₱2,500 received · today`). That bar is the point: without it an unpaid booking and a deposit-paid booking looked identical on opening, and staff had to read the receipts to tell which stage they were at. **The card has three faces, and only ever one at a time** — shown whenever the booking has charges (`paid + balance_due > 0`). **Open**: the full panel, while money is actually being taken — chip, `Pay by`, whole stay total, amount to pay, progress bar, reference box. It opens straight away for a booking with nothing recorded (after booking, payment IS expected) and otherwise **only when the staff decide to take the money**. **Waiting**: a calm amber strip — `[Partial] Partly paid · ₱700 of ₱1,400 received · ₱700 still to receive` — for a part-paid booking whose guest has not arrived. **Taking the rest is the staff's call, not a standing task**: an always-open payment panel made opening a deposit booking feel like being handed a job, so nothing is asked for here — no method, no reference, no amount. **Settled**: the green strip once nothing is left (`[Paid] Fully paid ✓ · ₱1,400 received · 2 payments · today`), with no figures and no bar, and deliberately no single method, because two may have been used. The **method** sits in the open panel's header. For the **first** payment it is **fixed text** (`Pay by CASH`), because the booking already asked the question — offering the picker again was noise staff could not act on. From the **second** payment on it becomes a picker, because the guest may settle the rest a different way (a cash deposit, then GCash at the door). Either way it decides what proof is asked for: **nothing for cash**, a **GCash reference box**, or a **bank transfer reference box**, with the missing-reference error inline beneath that box. The card is the only place the reference lives, and the button below submits it. It never disappears entirely — a settled booking looking like a different screen was the original complaint. Recording the money stays **one button below**, so there is a single loud action. The guest block also skips the whole contact line when the booking has no phone or email — the store writes the literal `None` when no phone was taken, so that placeholder never prints. This is followed by **one loud action, never two** — the card above already names the amount, the method and the reference, so no second form appears. While money is owed **and nothing has been recorded yet** (`hasPaymentRecorded`) it is the gold **"Confirm deposit paid & print receipt"** (reading "Confirm full payment & print receipt" for a pay-in-full booking): it takes the amount straight off the card, records the payment, auto-confirms the booking and **prints the Payment Receipt** — there is **no "Confirm without payment"** action, so a walk-in booking is confirmed only by recording money. Once the deposit is in and the rest is still owed (a deposit plan, guest not yet arrived) the button reads **"Receive money & check in"** — pressing it is the staff's *decision* to take the money, and it opens a [ReceivePaymentStep](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/ReceivePaymentStep.tsx) they have to complete, rather than a panel that just appears: **1** how the guest pays (asked again here, because the guest may settle the rest a different way), **2** the reference number when that method needs one (required, with its inline error), **3** the amount about to be received — then one confirm that records it, prints the receipt and checks the guest in. Cancel backs out. The card does **not** expand at the same time, so the method and the reference are asked for in exactly one place. With nothing owed the step is **Check in now**, then **Check out now** — so receiving or removing money brings the payment step back automatically. Everything else is collapsed behind [SlideOverSection](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/SlideOverSection.tsx) with a one-line summary while closed: **Payment receipts** ([BookingReceipts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/BookingReceipts.tsx) — record a payment, reprint any receipt), **Breakfast during the stay** (`BreakfastRecorder`, rooms only), and **Extend stay** ([ExtendStayForm](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/ExtendStayForm.tsx) — new check-out + extra-nights cost). Quiet footer: **Print billing statement** + **Cancel booking**. The statement opens from live booking state (never a stale copy), and **Check out now is blocked while any money is owed** — including late-checkout hours added at the moment of checkout itself — saying so **inline under the button** and opening the guided receive step so the money can be taken right there, rather than in a popup.
    - [ReceivePaymentStep](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/ReceivePaymentStep.tsx) — the guided arrival step in `ExtendStayModal`, opened by **Receive money & check in** (and by a blocked **Check out now** when the stay grew after check-in). Numbered in the order it must be done: how the guest pays, the reference where that method needs one, the amount to receive, then one confirm. Its amount is fixed — it is the rest of the bill, never typed — and the method is asked again here, unlike the booking's first payment.
    - [BreakfastRecorder](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/BreakfastRecorder.tsx) — day-by-day breakfast recorder for a room stay, shown in `ExtendStayModal`. Staff pick the day, a meal from the staff-editable menu, and how many people, and it's charged per person at check-out.
    - [LogOldBookingModal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/LogOldBookingModal.tsx) — single-screen entry to log a past paper booking (opened from the calendar toolbar's **Log old booking** button). Pre-fills **every room/venue** (Vacation House / Gazebo / Garden Area) picked on the calendar with its own dates, using a multi-unit editor ([OldBookingUnitPicker](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/OldBookingUnitPicker.tsx) — edit dates, remove, or add rooms/venues) and captures guest/companions + nationality, check-in/out dates, **Receptionist on duty** (`prepared_by`), the original paper **registration number** + date (`reference_number`/`registered_on`, kept so staff can find the physical log), and **what was paid** ([OldBookingPayment](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/OldBookingPayment.tsx) — a **Fully paid?** toggle that fills the full total into the editable **Amount paid** field (or type the amount you want); Total is auto-computed across all selected units from each unit's nightly rate × its nights — never typed, with a **Use Promo Price** toggle to pick Regular vs Promo; only the overall amount, mode, date are in). With 2+ units, one booking is saved per unit with the single amount paid split proportionally across them (payment receipt on the first).
    - [CalendarToolbar](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/CalendarToolbar.tsx), [CalendarLegend](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/CalendarLegend.tsx), [bookingStyles.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/bookingStyles.ts) — month toolbar (**New booking** and **Corporate**/partner joined as one segmented group — both need a picked date range first; **Log old booking** backfill is an icon-only button) , vertical left color/payment legend rail, floating multi-unit bar, and source/payment style helpers.
  - [BookingsListTab](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/BookingsListTab.tsx) - Money-first bookings list built for non-technical staff: big guest-name search, one-tap filters (`All stays` / `Who owes` / `Paid`), a "Who owes right now: N stays · ₱X" headline, plain-language payment badges (Owes / Partial / Paid), and per-row actions (Print invoice, Details). Upcoming stays sort first. Payment logic lives in `utils/bookingMoney.ts`; the detail view in `billing/BookingDetailsModal.tsx`. There is **no payment-status control** anywhere in the list or the detail view — the badge is read-only and follows the payments recorded on the booking.
  - [DirectoryTab](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/DirectoryTab.tsx) - Guests & Partners listing for guest stay records and agency/partner contract presets.
  - [SettingsTab](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/SettingsTab.tsx) - Settings panel for OTA iCal feeds and an editable **Rates** tab. It holds **Shared Rates** (late/early hourly + cap, breakfast menu (staff-editable items + prices), venue hourly, Gazebo/Garden day blocks, standard check-in/out, security deposit via `rateConfig.ts`) and a per-room **Room Rates** editor (`settings/RoomRatesEditor.tsx`) for each room's Regular `base_price` + Promo `promo_price`, saved through `updateRoomRate`.
  - [WalkInBookingForm](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/WalkInBookingForm.tsx) - The single booking form (default). Its extracted subcomponents/modules live in `src/components/walk-in/`: [PartnerBookingFields](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/walk-in/PartnerBookingFields.tsx) (corporate/agency panel), [BookingWizardHeader](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/walk-in/BookingWizardHeader.tsx) (header + 3-step indicator + Booking/Block toggle), [BookingCreatedPanel](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/walk-in/BookingCreatedPanel.tsx) (the printable Guest Billing Statement shown after a booking is created — **no payment is taken here**; the statement is handed to the guest first), [bookingEstimate.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/walk-in/bookingEstimate.ts) (`computeBookingEstimate` — pure wizard totals), and [bookingSubmit.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/walk-in/bookingSubmit.ts) (`submitBookingForm` — collision checks + per-unit create/update/cancel). The **individual** flow is a 3-step wizard: a **Booking / Block dates** toggle in the modal header; **Booking is the default** and **Block dates is a secondary action** that switches to a compact block-only mode (room/dates + block reason + Create Block, no guest/billing), then Step 1 = Guest Information (Name / Contact No. / Nationality / Address + Email & Birth date) & Companion Information; Step 2 = Add-ons & Discount (Amenities / DiscountPricingControls) + Receptionist on duty; Step 3 = Billing (Total / Deposit / Balance, with a **Use Promo Price** switch in the Billing header) → Confirm (or Create Block). After the booking is created the panel shows **only** the printable Guest Billing Statement — nothing sits above it. Money is never taken at that point: the guest is given the statement, and the payment is recorded afterwards from the booking's quick view, which is what prints their Payment Receipt. Corporate/partner bookings (`bookType === 'partner'` here, i.e. `bookingTy... (line truncated to 2000 chars)
  - [PrintInvoiceModal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/billing/PrintInvoiceModal.tsx) + [InvoiceDocument](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/billing/InvoiceDocument.tsx) - Print-ready **Guest Billing Statement** that mirrors the paper form staff fill in for walk-ins / Facebook calls. The controller (`PrintInvoiceModal`) builds the structured line items + totals via `utils/statement.ts` (`buildStatement`, which reuses `calculatePricing({ usePromo: booking.promo_applied })`) so the printed statement matches the booking's promo state; the presentational `InvoiceDocument` renders the form's sections (brand header + Bill/Invoice No. + Date Issued, guest info lines, stay details, companion table with Name/Nationality/Breakfast, a DESCRIPTION / QTY-NIGHT / UNIT / PRICE / DISCOUNT-Promos / AMOUNT charges table, payment-method + Sub-Total / Downpayment / Amount Due block, Prepared by + Guest Signature, and a Pension Policies block with standard check-in/out times). The companion table uses `c.nationality`/`c.breakfast` and the guest `birthdate`, and the **whole Companion block is dropped when the booking has no companions** — never printed as an empty table, which just wasted vertical space for a guest staying alone. The shared `Line` component **skips any field the booking does not hold** (trimmed, so whitespace counts as empty) instead of printing a label with a bare blank: Address, Email, Nationality, Birth Date, Sex, Contact No., Plate No., Room No. and Room Type drop out of the guest/stay blocks on their own. The only deliberate blanks remaining are the printed fill-in lines staff or the guest complete by hand: **Prepared by** (prints its rule so the name can be written in) and **Guest Signature**. There is no unit key/legend at the foot of the statement. The **Payment method is a REQUIRED field** on the booking form (the old "Not decided — print every way to pay" option is gone): without it the system cannot know how to verify the payment on arrival, so the form blocks on it via the standard inline `paymentMethodError`. The statement therefore always names a method. Only a booking saved before this rule shows the every-way-to-pay list, and that fallback stays purely defensive. **The statement prints only what applies** (owner's rule: never show a line that has nothing to say): only the **chosen** payment method is shown (never all four tick-boxes); **Account Details shows only the matching account** — the GCash name/number for GCash, the bank name/account for Bank Transfer, and nothing at all for cash — zero-value deduction rows (`Less: Downpayment/Deposit`, `Partial Payment`, `Other`) are hidden entirely, and the receptionist's `prepared_by` name is printed on the **Prepared by** line. **The bill answers the guest's own payment choice**: it prints the agreed **Payment Plan** (`Deposit (50%)` / `Full payment`) beside the method, and **Amount Due** is what the guest hands over NOW — the 50% deposit for a deposit booking, labelled under the figure — with the remainder shown beneath it as **Balance on arrival**. Printing a bare `balance_due` there made a deposit booking read as though the whole stay were due at once and contradicted what the guest chose; deposit + balance always add up to the Sub-Total. Match payment methods by whole word, not substring: `'gcash'.includes('cash')` is `true`, which previously ticked Cash on every GCash booking. Method matching and labels come from `utils/paymentMethod.ts` (`paymentKind` / `paymentMethodLabel`). The guest block prints **Sex** as well as Birth Date, Plate No., and the stay/companion details; a **Security Deposit** is printed as its own `Add:` row only when one is actually held.
  - [PaymentReceiptDocument](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/billing/PaymentReceiptDocument.tsx) + [PrintPaymentReceiptModal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/billing/PrintPaymentReceiptModal.tsx) — the printable **Payment Receipt** given to a guest after each payment. Unlike the blank paper receipt it only shows what actually happened: the one payment method used (with its reference), the actual payment-for reason (Reservation / Partial Payment / Room Accommodation), and the real status (Paid in Full / Partially Paid / Booked Confirmed / Venue or Room Reserved). Wired into `ExtendStayModal` so recording a payment immediately offers a receipt and every stored payment can be reprinted. **Each receipt carries its own stored number** (`PaymentRecord.receipt_number`, e.g. `PR-202608-001`, assigned by `utils/receiptNumber.ts` when the payment is recorded) — never a number derived from the payment's position in the list, which used to renumber every later receipt as soon as one was removed. Payments recorded before numbering existed fall back to a derived number. "Previous Balance" reconstructs what the guest owed before this payment by splitting `downpayment_paid` into a booking-time deposit plus the recorded receipts, so legacy and imported bookings that paid without a stored receipt still show the right figure. The method shown is the payment's own method, matched by whole word via `utils/paymentMethod.ts`. **`Received From` and `Received By` are section headings, never fields**: a heading says what the labelled lines under it represent, so it carries no value of its own. Filling `Received From` with the guest's name printed that name twice (once there, once as the guest line) and told the reader nothing; the block now reads heading → `Guest:` / `Room No:` / `Status:`. **It prints on the hotel's 58 mm thermal roll**, so the slip is a **single column** (no side-by-side tables — a label and a long value cannot share one row at that width), **pure black on white** (the printer is 1-bit, so grey text only dithers into fuzz), 8px–17px type, and nothing wider than `w-[58mm]`; anything that can run long (guest name, invoice number, reference) gets its own `Block` line. The **screen preview is that same 58 mm slip**, so what staff see is what comes off the printer. `PrintPaymentReceiptModal` injects `@page { size: 58mm auto; margin: 3mm }` **only for the duration of the print** and removes it again, so the A4 Guest Billing Statement keeps its own page size.
  - [BookingDetailsModal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/billing/BookingDetailsModal.tsx) - Compact read-only booking details modal (guest, dates, unit, invoice, source, phone, email, GCash ref) with Print invoice.
  - [LoginPortal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/LoginPortal.tsx) - Staff passcode validation gate component.
  - [MainLayout](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/MainLayout.tsx) - Wrapper providing basic container styling.
  - [Toast](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/Toast.tsx) — the single place the staff app speaks to the user: `<ToastHost />`, mounted once in `DashboardLayout`, renders whatever [`utils/toast.ts`](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/toast.ts) currently holds (`showToast(text, 'success' | 'error' | 'info')`, auto-dismissed after ~3.2s, errors after 5s). It is bottom-centre so it clears both the header tabs and the calendar's top-right sync notice. **There are no `alert()` calls anywhere in the app** — a browser alert freezes the whole app until it is dismissed and does not say which action produced it.
  - [ConfirmDialog](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/ConfirmDialog.tsx) — the app's own "are you sure?": `<ConfirmHost />`, mounted beside `<ToastHost />` in `DashboardLayout` at `z-[70]` so it also sits above the booking slide-overs (`z-50`) that ask their own questions. Cancel on the left, the action on the right — red for destructive, gold otherwise — and **Escape always means no**.
- **Data Operations**:
  - [syncEngine.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/syncEngine.ts) is a barrel re-export. Actual logic lives in domain modules under `src/utils/`:
    - [helpers.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/helpers.ts) — `generateUUID`, `randomUUID` (real RFC-4122), `isValidUUID`, `normalizeVenueId`.
    - [defaultData.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/defaultData.ts) — `DEFAULT_ROOMS` and `DEFAULT_VENUES` seed data.
    - [db.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/db.ts) — All CRUD operations (getRooms, updateRoomRate, getVenues, getBookings, saveBookings, insertBooking, updateBooking, deleteBooking, confirmBooking, getFeeds, saveFeeds) with Supabase primary + localStorage fallback. Supabase writes route through Postgres RPCs (`book_booking`, `update_booking`, `delete_booking`, `confirm_booking`, `cleanup_expired_pending`); business-rule failures (ROOM_UNAVAILABLE / VENUE_UNAVAILABLE / invalid dates) surface to the UI instead of falling back to localStorage. Non-business-rule write failures (e.g. the database being unreachable) fall back to the browser store so the app still saves offline; business-rule failures (ROOM_UNAVAILABLE / VENUE_UNAVAILABLE / invalid dates) surface to the UI. `update_booking`/`delete_booking`/`confirm_booking` are invoked for every stored text id (including non-UUID `manual-…`/`imported-…` ids, since `bookings.id` is a TEXT column), so legacy ids persist correctly; only `insertBooking` normalizes *new* ids to real UUIDs. Sequential invoice numbers retry on unique-violation races. Also `initDB()` for first-run seeding. `updateRoomRate` saves a room's Regular/Promo rate through the `update_room_rate` SECURITY DEFINER RPC (RLS gives anon SELECT-only on `rooms`), falling back to a localStorage override (`l_etoile_rooms_db`) that `getRooms` overlays for offline use.
    - [availability.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/availability.ts) — `isRoomAvailable`, `isVenueAvailable`, `isVenueRangeAvailable` collision detection.
    - [pricing.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/pricing.ts) — `calculatePricing`: client-side pricing policy (explicit **Regular/Promo** mode via `usePromo`; nightly rate is `promo_price` when `usePromo=true` else `base_price`, 50% reservation deposit, equipment rental rates, event add-ons; legacy `rateMultiplier` retained only as a shim for partner/additional-discount stacking). It also takes `appliedDiscount` (20%/10% percent or flat peso custom from `AppliedDiscount`), `earlyCheckInHours`/`lateCheckOutHours` (₱/hr up to `lateEarlyCapHours` then 1 night; vacation house is always hourly), `venueDayBlocks` (Gazebo/Garden 6-hour day blocks), `breakfastRecords` (per-day breakfast recorded during the stay — charged per person at check-out; legacy `breakfastDays`/plan-ahead remains as a fallback), and `rates` (a `RateConfig`; defaults to `DEFAULT_RATE_CONFIG`).
    - [promoMode.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/promoMode.ts) — Global promo-sale mode persisted in `localStorage` (`daweez_promo_active`), `isPromoActive`/`setPromoActive` + `getEffectiveNightlyPrice(regular, promo, promoOn)`; broadcast via `storage` and `promo-toggle` events for cross-tab sync.
    - [otaSync.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/otaSync.ts) — `runSimulatedOTASync` for iCal feed ingestion. Mock OTA booking injection is gated behind `VITE_ENABLE_MOCK_SYNC` (off by default) so the local/offline fallback never fabricates fake bookings; the fallback is non-destructive.
    - [seedData.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/seedData.ts) — `seedFutureMockData` for testing with June–Dec 2026 bookings.
    - [partnerDeals.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/partnerDeals.ts) — Partner deal CRUD with Supabase/localStorage dual path.
    - [expenses.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/expenses.ts) — Expense categories and expenses CRUD with Supabase/localStorage dual path.
    - [bookingMoney.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/bookingMoney.ts) — Plain-language payment views for staff: `getPaymentView` (Paid / Partial · ₱X left / Owes ₱X / Not paid — the amount stays IN this label for the bookings list, where the badge is the only place the money appears), `paymentStatusWord` (the same status with **no amount in it**, for the quick view whose card shows the money as its own big number, so one figure never reads as two), `paymentPlanLabel`, `hasPaymentRecorded` (true once any money has landed), `isOwed`, `hasOutstandingBalance` (the money gate: it blocks **check-in**, where the balance is received on arrival, and still guards check-out), `amountToPayNow` (what to RECEIVE now — the agreed 50% deposit or the full amount while nothing is paid, switching to the outstanding balance once a payment is recorded, so a settled deposit never shows ₱0 and hides money still owed), plus `PAYMENT_BADGE_CLASSES` (vibrant green paid, amber partial, red owes).
    - [paymentMethod.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/paymentMethod.ts) — the one place that decides what a payment method is: `paymentKind` / `paymentMethodLabel` / `methodNeedsReference` (whole-word matching, so `'gcash'.includes('cash')` can never tick Cash). Receipts and statements read the method belonging to each recorded payment (`record.method` through these helpers), never the method the guest merely agreed to at booking time.
    - [receiptNumber.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/receiptNumber.ts) — `nextReceiptNumber` (assigns the stored `PR-YYYYMM-NNN` number when a payment is recorded), `receiptNumberFor` (shows it, falling back to a derived number for payments recorded before numbering), and `paymentBreakdown` (splits money received into a booking-time deposit plus recorded receipts so a receipt's Previous Balance is right for legacy bookings too).
    - [statement.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/statement.ts) — `buildStatement`: turns a booking (and any related bookings sharing an invoice number) into structured line items + payment totals for the printable Guest Billing Statement (stay rows, one row per per-day breakfast served, extra linen/rental equipment, venue excess hours, event add-ons, early/late check-in-out, staff discount), reusing `calculatePricing`. `dateIssued` carries the bill date + time. Its `amountDue` is what the guest hands over at this point — the agreed 50% deposit while nothing is paid, otherwise what is left (`amountToPayNow`, never the whole stay) — with `balanceAfter` for the rest and `paymentPlan` carrying the guest's own choice through to the printed page.
    - [rateConfig.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/rateConfig.ts) — editable `RateConfig` (late/early per hour + cap, staff-editable breakfast menu (`breakfastMenu`: name + price per item; default matches the paper form — Hotsilog, Bangsilog, Lumpiasilog, Cornsilog, Milo, Hot Coffee), venue hourly, Gazebo/Garden day-block hours (6) + rate, security deposit, standard check-in/out times, and extras/rental rates — foam/pillow/blanket/towel, big/small table, chair, mineral water, tent) with `DEFAULT_RATE_CONFIG`, `getRateConfig`, `saveRateConfig` (localStorage).
    - [paymentAccounts.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/paymentAccounts.ts) — where the guest sends a downpayment (GCash name/number + bank name/account). Defaults match the official Daweez form (GCash Narlina D. · 0910-793498, BPI Daweez Pension House · 5636-0544-12); editable in Settings → Rates via `getPaymentAccounts`/`savePaymentAccounts`, and shown on the guest portal, chatbot, printed Guest Billing Statement, and the booking Money step.
    - [checkInOut.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/checkInOut.ts) — `computeCheckInOutHours`: computes early check-in / late checkout hours from the actual check-in/out times against the standard times (default 2 PM check-in, 12 PM check-out), rounding partial hours up.
    - [inventory.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/inventory.ts) — hotel inventory items (pillows, blankets, soap, etc.) with `getInventory`/`saveInventory` (Supabase + localStorage dual path).
    - [cleaning.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/cleaning.ts) — cleaning checklist entries (item, room, date, `cleaned_by`, `checked_by`, status) with `getCleaningTasks`/`saveCleaningTasks` (Supabase + localStorage dual path).
    - [toast.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/toast.ts) — the message store behind `Toast`: `showToast(text, tone)` from any screen, `useToastMessage()` for the host. Deliberately a plain module singleton (no provider, no drilled prop), and deliberately **not** `window.alert`.
    - [confirm.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/confirm.ts) — `askConfirm({ title, message?, confirmLabel?, tone? })` returns a promise for the answer, so a destructive call site still reads as one line: `if (!(await askConfirm({ title: 'Delete this expense?', confirmLabel: 'Delete', tone: 'danger' }))) return`. Asking a second question resolves the first as "no" so no caller is left waiting forever.
  - [DiscountPricingControls](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/DiscountPricingControls.tsx) — the money inputs in the booking form's add-ons step: staff discount (None/20%/10%/Custom flat price) and Gazebo/Garden day blocks. Early/late check-in-out is **not** set here — see the check-in/out pricing note.
  - [HousekeepingTab](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/HousekeepingTab.tsx) — the `/housekeeping` tab combining **Hotel Inventory** (add/remove items, adjust counts) and **Cleaning Checklist** (log item + who cleaned + who checked).
  - [useBookings.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/hooks/useBookings.ts) custom React hook orchestrating all React Query operations, exposing partner preset deals and CRUD mutations (website creation passes `usePromo` from `promoMode`, manual creation persists `promo_applied`; pending-booking creation 30-min hold).
  - [useRealtimeBookings.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/hooks/useRealtimeBookings.ts) hook that listens to PostgreSQL replication changes over WebSockets and updates the React Query bookings cache in real-time, mapping ALL booking columns (guest demographics, payment, invoice, partner, contract, `promo_applied`) so live UPDATE events never strip fields from the cache.

## Work Guidance

- **Styling Rules**: Tailwind CSS variables + the **Daweez charcoal-gold palette**, sampled from the brand logo (charcoal `#1C1C1C`, gold `#D0AB60`). Ramps: `ink` (neutral charcoal 50-950), `gold` (100-900; `gold-400 #D0AB60` is the brand gold, `gold-800 #6F5420` is "deep gold" and the only gold safe as TEXT on light surfaces), `paper` (ivory 0-500 for the light workspace), `danger` (50-600, the "needs attention" ramp — named `danger` so it cannot collide with daisyUI's own `error`). Semantic tokens live in `src/index.css`: `--brand-primary #D0AB60`, `--brand-text #6F5420`, `--brand-ring #1C1C1C`, `--bg-page #F8F5EF`, `--body-bg #EFEBE3`, plus the `--chrome-*` set (charcoal surface + `chrome-accent` gold), used ONLY where a dark brand moment is wanted: the login screen ([LoginPortal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/LoginPortal.tsx) — gold monogram in a circle + uppercase wordmark, echoing the logo) and the public reservation portal header ([PublicReservePortal](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/PublicReservePortal.tsx)). **The staff app shell stays LIGHT** — [DashboardLayout](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/DashboardLayout.tsx) header and mobile bottom nav use `bg-card` + `border-soft`, not chrome (owner preference: keep the working shell in light mode; a dark navbar was tried and reverted). The old light/dark `daweez_theme` toggle was removed along with the `.dark` block, so there is currently **no** dark-mode toggle. **Teal is retired.** Contrast rules: `#D0AB60` is only 2.2:1 on white, so it is NEVER text on light — use `gold-800` (`text-brand-text`); gold FILLS always take charcoal labels (`text-ink-900`, 7.9:1), never `text-white`; bright gold (`gold-400`/`chrome-accent`) as text/icon is only valid on the dark chrome (7.9:1). Fonts: Sora via `font-display` for headings, DM Sans via `font-sans` for body. Avoid raw inline styles. Use `rounded-lg`/`rounded-xl` fr... (line truncated to 2000 chars)
- **Dashboard Grid Layouts**: When creating 2-column split views (e.g. Form on the left, Data Ledger on the right), wrap the page title and description within the left column container. Do not span the title across the top of the grid, as this pushes the data column down and breaks vertical centering.
- **Settings Space Efficiency**: For repetitive configuration sections (like room settings or long URLs), prefer dense accordion lists over stacked padded cards. Use absolute-positioned action buttons (e.g. copy icons) embedded directly inside input fields to save horizontal space.
- **Readability & Badges Column Layout**: Do not apply restrictive max-width constraints to table columns that display badges or tags. Overrides and status indicators must render in clear, well-spaced badges using brand accent tokens (`brand-text`/`brand-bg`/`brand-border`, gold) for maximum readability.
- **File-Size Flag**: `ExtendStayModal.tsx` and `WalkInBookingForm.tsx` exceed the 300-line limit and are flagged for refactoring into subcomponents (per the project file-size rule). `ExtendStayModal.tsx` was partially refactored (512 -> 472 lines) when its layout was redesigned: `BookingMoneyPanel`, `BookingReceipts`, `ExtendStayForm` and `SlideOverSection` are now separate; the remaining bulk is its own handlers/derived values, which would need a hook to go further. `CorporateBookingForm.tsx` was extracted from `WalkInBookingForm` to keep booking concerns separate. `WalkInBookingForm.tsx` was partially refactored (1089 -> 618 lines): its partner branch, wizard header, and post-booking payment panel are now separate components, and `bookingEstimate.ts` / `bookingSubmit.ts` hold the pure estimate and submit logic. The remaining split (form state/derived values into a hook, plus context-fed step bodies) is intentionally PAUSED, not pending: at ~620 lines the file is fully readable in a single read (well under the ~50 KiB read cap that caused the earlier truncation), so the extra restructuring is deferred until the booking form's development settles. Do not resume it unprompted; revisit if the file grows past ~50 KiB.
- **Bookings Start Unpaid (Option B) + automatic payment status**: A new booking is saved with **no money received** (unpaid, owes the full amount). The flow is: create the booking → give the guest the printable **Guest Billing Statement** → they pay → staff record the payment from the booking's quick view. Recording a payment appends a dated `PaymentRecord` and immediately offers the printable **Payment Receipt**.
  - **Status on create**: a new **walk-in** booking saves as `status: 'pending'` — shown to staff as **"Unpaid"** (amber badge in the bookings list; "On hold" on the calendar pill). It **still blocks the room** (availability ignores status) and does **not** expire (`expires_at` is null). **Corporate/partner** bookings save as `confirmed` (contract-backed). **Editing** a booking preserves its existing status — it never silently confirms.
  - **Payment status is never typed by hand**: there is **no payment-status dropdown anywhere** — not in the quick view, the bookings list, the booking details modal, or the calendar pill. The status (`unpaid` / `downpayment` / `paid`) is **derived from the money actually recorded**: no payment → Unpaid, part of it → Deposit, all of it → Fully paid. Every reading is read-only (badge, payment dot) and follows the balance. To correct a mistake, **remove the wrong payment** (trash icon in the quick view's Payment receipts) — the balance and the status recompute themselves. Only the **Log old booking** backfill still takes an entered amount, because a paper log has no digital receipts.
  - **Balance restore**: because the status follows the money, `balance_due` must always be recomputed from the booking's own charges (`withRecomputedBalance`) whenever a payment is added or removed — never left as a stale zero. The quick view reads live state so badge, amount and next step update at once.
  - **Auto-confirm**: the first payment recorded — a 50% deposit *or* the full amount — flips `pending → confirmed` via `statusAfterPayment` in [bookingStatus.ts](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/utils/bookingStatus.ts). The quick view is now the **only** payment entry point, so the rule lives in one place.
  - **Step 3 captures no payment, but does capture the plan**: it shows an estimate plus **What the guest pays now** — a two-button choice between **Deposit (50%)** and **Pay in full**, saved as `payment_plan` ('deposit' | 'full') — and the **Payment Method, which is REQUIRED** (left blank the form refuses to save, via the inline `paymentMethodError`) — the system has to know how the payment will be verified on arrival. Both are printed on the statement and read back in the quick view; no money is taken and no payment reference is stored for an unpaid booking.
  - Recording a payment in the quick view needs **no form**: the money card already holds the amount, the **Pay by** method and the reference, so the shared [RecordPaymentForm](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/calendar/RecordPaymentForm.tsx) survives in **one** place only — the **Payment receipts** block, for an in-stay charge or a part-payment where the staff member types the amount from scratch. **The rest is received on arrival** by the single button described below.
  - **The deposit holds the room; the rest is received at check-in**: the 50% deposit exists only to reserve the room and the guest's name for those dates. **The rest is received by the "Receive money & check in" button** described in the Work Guidance bullet below. There is no separate arrival form on screen until it is needed: pressing that button opens the guided **ReceivePaymentStep**, which carries the amount, the method and — for GCash/bank — the reference. A "pay in full" booking never sees two steps, because its whole amount is the booking deposit. The amount must stay the **agreed** one (`amountToPayNow`, the 50% deposit while nothing is recorded), so a 50%-deposit guest at the desk is never asked for the full stay by default.
  - **The booking-stage payment decision is never asked twice**: the quick view does not re-open a form for the booking payment. The money card already carries the agreed amount in **Amount to pay** and the agreed method as fixed text, so the only thing left is the single **"Confirm deposit paid & print receipt"** button under it (reading "Confirm full payment & print receipt" for a pay-in-full booking). There are **no "50% deposit" / "Full amount" shortcuts** anywhere — that decision belongs to the booking form. Wording is **"Receive", never "collect"/"take"** — the money arrives FROM the guest. `RecordPaymentForm` keeps its fixed shape (title, one short `note` line, Amount + Method side by side, Reference, submit) and checks the Amount **itself**, inline under the box; its submit button is bound as `onClick={() => submit()}`, never `onClick={onSubmit}` — see the click-wiring rule under Performance Guardrails.
  - **Consequence to know**: because unpaid walk-ins are `pending`, they are **excluded from the Earnings Report** unless **Include Pending Bookings** is switched on (Analytics filters).
- **Free-Typing Number Boxes**: Money/quantity inputs use the shared [NumInput](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/NumInput.tsx) (or a plain `type="text"` + `inputMode` box). Never use `type="number"` — the OS spinner and its "snap back to 0 on backspace" behavior confuse staff. NumInput types freely (Backspace clears it, can be empty) and still reports a clean number via `onChange`.
- **Money-First Payment Display**: Staff-facing payment info must read like plain language, never accounting columns. Use big, bold, color-coded labels — vibrant green for "Paid", amber for "Partial · ₱X left", strong red for "Owes ₱X" — via `utils/bookingMoney.ts`. Keep the single most important money number (e.g. "Who owes right now") visible at the top of money-related lists.
- **Always Apply UX Best Practices**: Every UI must be usable and space-smart by default — high information density, no wasted white space (never leave a dead row next to a small control), minimal scrolling (no "box on box" stacking), progressive disclosure (show rare/secondary options only when relevant; default to the common path rather than forcing a choice), multi-column/responsive grids to use horizontal width, clear labels/affordances/feedback, and accessibility. Prefer familiar patterns and keep it compact.
- **Report extras are configurable per group (Earnings Report)**: The report's **Extras** column shows a flat staff-editable amount per category group — **Rooms + Vacation House** share one (`RateConfig.accommodationExtras`), **Garden + Gazebo** share another (`RateConfig.venueExtras`), set in **Settings → Shared Rates**. These REPLACE the per-category extras figure (rooms no longer itemize booking extras in the summary; venues' "Included" becomes this amount) and are ADDED to each category's total. A category's Room Rate column stays the base, so Extras + base = Total. Per-room drill-down rows still show their real booking extras.
- **Inline Required-Field Validation**: Booking forms validate required fields per field, not as one message at the bottom. Required boxes keep a red `*` label, get a red border (`border-danger-400`), and show a short red note right under the box when left empty — it fires live on blur and again on Save (mark empty fields with a touched/trySave flag). This keeps the note where the problem is instead of a single generic error.
- **Responsive Design**: Desktop-first layout. Top tab nav is the primary navigation. The UI is built for larger screens first with responsive adaptations for mobile. Bottom tab nav appears on mobile (`md:hidden`), while top tabs are always visible on desktop (`hidden md:block`). Tables use `overflow-x-auto` for horizontal scrolling on small screens. Calendar cells use responsive min-heights (`min-h-[80px] sm:min-h-[100px]`). On the `/calendar` route, the layout is viewport-locked (`h-screen overflow-hidden`) to eliminate dual scrollbars, keeping top controls and table headers stickily anchored while allowing the grid table itself to scroll horizontally and vertically.
- **Paper Guest Registration as Reference**: The staff-familiar paper Guest Registration form (Daweez Pension House) is the reference for the walk-in booking UI and the printed Guest Billing Statement — same fields (Birth Date, **Sex**, **Plate No.**, companion Name/Nationality/Breakfast), payment methods (Cash/Gcash/Bank Transfer/Other), Pension Policies, standard check-in/out times, and a Guest Signature line. Keep the digital form faithful but simple so staff recognize it. Print a field only when the booking actually holds it (never a label with nothing under it).
- **One Payment, One Numbered Receipt**: Every payment — walk-in, in-stay, or taken online at booking — creates its own `PaymentRecord` carrying a **stored** `receipt_number` (`PR-YYYYMM-NNN`). Never derive a receipt number from the payment's index: removing one payment would renumber every later receipt, and a number staff quote to a guest must stay stable. Before adding a payment, check `methodNeedsReference` (`utils/paymentMethod.ts`) — GCash and bank need a reference, cash does not. **Every payment carries its OWN method.** The first payment uses the method agreed when the booking was made — printed on the card as fixed text, never asked again, since the booking form already captured it. From the **second** payment on, the quick view's **Pay by** picker appears so the guest can settle the rest another way, the record stores the method actually used, and the booking's `payment_method` follows the latest one. Never print one method for money that arrived two ways — the receipts each name their own.
- **Money is taken twice: deposit at booking, balance at check-in**: the **deposit reserves the room and the guest's name** for those dates and nothing more. When the guest arrives there is **no second form**: the quick view's money card already holds the amount and, for GCash/bank, the reference. **Check in is one button, not a dead end**: while the bill is unpaid the Check in button is **not rendered at all** — the money card above carries the next payment. Once the deposit is in and the rest is owed, the button reads **"Receive money & check in"** — that press is the staff's decision, and it opens the guided **ReceivePaymentStep** (method → reference if needed → amount → confirm) rather than a panel that just appears; confirming records that amount (`handleAddReceipt(due, true)`), prints the receipt and calls `performCheckIn`. With nothing owed the button becomes **Check in now** — "nothing owed" alone is the test, so a booking whose money was recorded before receipts existed (imported / old paper logs) still gets its arrival button and is never left with no next step at all. Wording is **"Receive", never "collect"/"take"**: the money arrives FROM the guest, so the hotel must not read as holding a balance of theirs. **Check out keeps its own money check** as a backstop for a stay that grew after check-in (extra nights, per-day breakfast); late-checkout hours are applied first, and if they re-open the bill the guest must settle before leaving. Walk-in bookings are confirmed only by recording money — there is no "confirm without payment" path.
- **Money reads come from the money recorded**: a receipt or statement takes its method, reference and Previous Balance from that payment's own `payment_records` entry — never from the method the guest merely agreed to at booking time (`paymentKind`/`paymentMethodLabel` in `utils/paymentMethod.ts`, `paymentBreakdown` in `utils/receiptNumber.ts`). The quick view does **not** summarise what has been received line by line: the **Payment receipts** block already itemises every payment with its amount, method, date and time, so the money card shows the two figures plus the progress bar, and nothing is said twice. **One input per value**: the money card owns the reference box for the payment about to be taken (it appears only for GCash/bank) and the single button below submits it, so the same number is never asked for twice.
- **Standard Check-in / Check-out Times**: Check-in is 2 PM and check-out is 12 PM by default. The Check in / Check out buttons compute early/late hours automatically against these (configurable in Settings→Rates as `standardCheckInTime` / `standardCheckOutTime`). Early/late is charged **only at the actual check-in/out** (rooms ₱/hr up to `lateEarlyCapHours`, then one full night; venues charge the venue hourly rate, e.g. ₱500/hr, hourly for any overlap incl. Gazebo & Garden) — never during booking, because the arrival/departure time isn't known yet.
- **Double Booking Prevention**: Any transaction or booking action must run collision checks through `isRoomAvailable` or `isVenueRangeAvailable`. Check-in dates must be chronologically earlier than check-out dates.
- **Group Selection Mode**: When booking multiple units in Group Selection Mode, each unit has its own check-in/out date range selected sequentially on the timeline grid (2 clicks per unit: check-in → check-out). Toggling or selecting ranges runs immediate collision checks; conflicts trigger warnings but do not clear the active selection list. Picks accumulate silently (no floating selection bar); the **New booking**, **Corporate**, and **Log old booking** buttons stay disabled until at least one unit/date range is picked, then clicking the enabled one continues into the full booking form (or the corporate form) with those picks. Launching any booking action — **New booking**, **Corporate**, or **Log old booking** — clears the picked selections off the calendar so they don't stay highlighted after.
- **One Booking Process**: The single booking form is `WalkInBookingForm` (a 3-step wizard; the individual flow, partner uses `CorporateBookingForm`), used for every way of creating or editing a booking (New booking button, click-empty-day, group selection, and editing). The room/dates come from the calendar pick (the entry point); the modal header has a **Booking / Block dates** toggle, and Block is a secondary action that shows a compact block-only mode. Step 1 captures guest information (Name / Contact No. / Nationality / Address) + companions; Step 2 captures Add-ons & Discount + Receptionist on duty; Step 3 captures the read-only Billing summary (Total / Deposit / Balance), then Confirm. A new booking is saved **not yet paid** (Option B); money is counted only when staff record a payment from the booking's quick view (which appends a `PaymentRecord` and prints a Payment Receipt). `createManualBooking` stays the single write path (collision checks + pricing inside).
- **Old Booking Backfill (paper logs)**: Use the calendar toolbar's **Log old booking** to import a historical paper booking. The system still assigns its own sequential `invoice_number` (GRF-YYYYMM-NNNN), and the original paper log number + date are stored in `reference_number`/`registered_on` and included in the bookings-list search (so staff find a booking by guest name, the system number, or the old paper number). What was paid is recorded via `downpayment_paid` / `balance_due` / `payment_status` and a `payment_records` entry. The form accepts **multiple rooms/venues** at once (pre-filled from the calendar selection). The **Total is auto-computed** across all selected units (each unit's nightly rate × its nights), with a **Use Promo Price** toggle to choose Regular or Promo (defaults to Regular) — staff only type the amount actually paid, which is split proportionally across the bookings; contact number is optional (a paper log may have only a guest name).
- **Per-Day Breakfast (not planned ahead)**: Breakfast is **not** chosen at booking time. Staff record it day-by-day during the stay in the booking details modal (`BreakfastRecorder`) — pick the day, a meal from the staff-editable menu (`getBreakfastMenu()`), and how many people; each entry stores its price so the menu can change later without re-pricing. It's added to the balance and appears on the billing statement at check-out. Old plan-ahead bookings still bill via the legacy path.
- **Null-Safe Pricing Reads**: `calculatePricing` treats `null` like `undefined` for `breakfastOrders` (DB round-trips turn missing arrays into `null`); callers passing stored booking fields into the invoice must normalize `null` arrays (PrintInvoiceModal maps `null` → `[]`) so re-priced invoices never crash or invent charges.
- **Local Date Strings (timezone-safe)**: Booking dates (`check_in`/`check_out`) are plain `YYYY-MM-DD` calendar-day strings. Always build them with `dateToString(date)` from `utils/helpers.ts` (local components) — never `.toISOString().split('T')[0]`, which shifts dates back one day in UTC+8. This applies to calendar cell keys, booking creation/confirmation, "today" comparisons (briefing, dashboard stats, bookings list), and date-input defaults/minimums.
- **Regular/Promo Pricing**: Each room/venue stores a `base_price` (Regular) and optional `promo_price` (exact sheet price). The nightly rate is `promo_price` when `usePromo=true` else `base_price`; `calculatePricing({ usePromo })` reports `discountAmount`/`discountPercent` from the exact regular-vs-promo delta (not a fixed 20%). The website uses promo only while the global `promoMode` flag is ON and persists `promo_applied` onto the booking; the walk-in form uses a per-booking **Use Promo Price** toggle that likewise persists `promo_applied`; old automatic 20% for `website/manual/facebook` is removed. Read paths (invoice, analytics) recompute via `booking.promo_applied` so historic and new invoices remain stable.
- **Offline / Local Fallback**: Code must check `isSupabaseConfigured` and seamlessly fallback to local storage DB endpoints (`l_etoile_bookings_db`, `l_etoile_feeds_db`) to guarantee runtime persistence without a live database.
- **Background Calendar Sync**: Third-party bookings (Airbnb/Booking.com) must sync automatically via iCal feeds in the background upon mounting the application and at regular intervals (5 minutes) thereafter, with a manual OTA Sync trigger as a backup.
- **Staff Passcode Authentication Gate**: The staff portal is protected by a client-side passcode authentication gate. Authorized passcodes include 'daweez2026' (case-insensitive), '8888', and any custom passcode defined in VITE_STAFF_PASSCODE. Successful authentication persists in localStorage as `daweez_pms_auth` until explicit logout.
- **Performance Guardrails for Grids & Complex Views**:
  - **State Isolation**: High-frequency states (e.g. mouse hover coordinates, hovered dates, range calculations) must live at the lowest leaf component level possible. Avoid placing hover states in orchestrators or context providers to prevent wide component trees from re-rendering.
  - **Memoized Cell Wrappers**: Wrap individual grid cells or list items in a memoized component wrapper (`React.memo`) using strict custom equality comparators to bypass rendering. Ensure callbacks are stable or checked appropriately, converting $O(N)$ re-renders to $O(1)$ changes. A memo comparator MUST compare every prop that affects the rendered output or an onClick closure (e.g. the cell's `date`, `isoStr`, `id`), and key dynamic lists by stable data identity (e.g. the day's ISO string), NOT array index — otherwise a reused component instance keeps a stale value in its handler after data changes (this caused the timeline to sometimes save a wrong, month-shifted check-out date like Aug 20 when July 20 was clicked).
  - **Numeric Timestamp Operations**: Avoid using string parsing operations (like `.toDateString()` or `.toISOString()`) inside rendering loops. Convert dates to numeric timestamps via `.getTime()` for cheap integer comparisons.
  - **Keystroke Rendering Isolation**: Keep text field keystroke states isolated so typing in registry forms does not trigger re-calculations or re-renders of adjacent sidebar receipts and summaries.
  - **Avoid Backdrop Filter Penalty**: Do not apply `backdrop-filter: blur(...)` styling to modals or overlay backdrops rendered on top of dense grids or tables. This triggers heavy GPU composition paint operations on every keypress or state change. Use solid or semi-transparent flat colors instead (e.g. `bg-slate-900/50`).
  - **Debounced Hover Tooltips**: Details tooltips on dense timeline grids must be debounced with a short delay (e.g., `500ms`) using a `useRef` timeout to prevent rendering spam when moving the mouse across cells. Stale timeouts must be cleared on mouse leave and on component unmount.
  - **Floating UI Alerts over Inline Banners**: Transient states (such as active scheduler selection status) must be presented in absolute/fixed floating elements (toasts) rather than inline blocks to avoid grid layout shifts, vertical screen clutter, and component displacement.
  - **No Browser Dialogs — ever**: `alert()`, `confirm()` and `prompt()` block the entire app, steal focus out of the box staff were typing in, and never say which action raised them. Everything a popup used to say has a proper home: a **field** problem (missing amount, missing GCash reference) is shown **inline under that box**; a problem with a **button just pressed** (checking out while money is owed) is shown **inline under that button**; anything else transient ("Partner deal saved.", "Could not save the rates.") goes through **`showToast`**. **Destructive actions ask through `askConfirm`** (`utils/confirm.ts` → [ConfirmDialog](file:///c:/Users/dev4s/Documents/Programming/Daweez/src/components/ConfirmDialog.tsx)): deleting an expense, a category or a partner profile, removing a payment, cancelling a booking. The codebase contains **no native browser dialogs at all** — `alert`, `confirm` and `prompt` are all gone.
  - **Never pass a parameterised handler straight to `onClick`**: `onClick={onSubmit}` hands the DOM **click event** to `onSubmit` as its first argument. When that argument is an optional override — as in `handleAddReceipt(amountOverride?, thenCheckIn?)` — the event (a `MouseEvent`) silently becomes the amount, `Number(event)` is `NaN`, and a form that already showed ₱1,250 reported "enter a payment amount" and refused to save. Always wrap it: `onClick={() => onSubmit()}`, and keep the handler defensive by accepting an override only when `typeof it === 'number'`.
  - **Lazy-Route Dependency Pre-Bundling**: Dependencies reachable only through a lazy-loaded route (e.g. `@tanstack/charts` on `/analytics`) must be listed in `vite.config.ts` → `optimizeDeps.include`. Otherwise Vite discovers them on first navigation and answers `504 Outdated Optimize Dep`, which fails the dynamic import with `Failed to fetch dynamically imported module`.

## Verification

- **Code Validation**: Verify TypeScript build compatibility using `npm run build` or checking Vite compile output.
- **Responsive Views**: Check that all UI elements adapt correctly on mobile, tablet, and desktop viewports.
- **Desktop Build**: Verify that you can compile the native desktop client using Deno by running `C:\Users\dev4s\.deno\bin\deno.exe task desktop:build` and checking that the resulting folder structure is generated at `dist/DaweezPMS/`.

## Child DOX Index

No child DOX documents reside under `src/`.
